import { PmaoDirective } from './pmao.directive';

describe('PmaoDirective', () => {
  it('should create an instance', () => {
    const directive = new PmaoDirective();
    expect(directive).toBeTruthy();
  });
});
