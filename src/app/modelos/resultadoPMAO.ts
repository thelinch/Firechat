import { file } from "./File";
import { FileUploadWithPreview } from 'file-upload-with-preview';

export class resultadoPMAO {
  resultado: number
  estado: boolean
  fecha_registro: firebase.firestore.Timestamp
}
