import { TransformSubactividadesPipe } from './transform-subactividades.pipe';

describe('TransformSubactividadesPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformSubactividadesPipe();
    expect(pipe).toBeTruthy();
  });
});
