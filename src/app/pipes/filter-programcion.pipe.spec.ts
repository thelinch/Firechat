import { FilterProgramcionPipe } from './filter-programcion.pipe';

describe('FilterProgramcionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProgramcionPipe();
    expect(pipe).toBeTruthy();
  });
});
